import { Default } from 'components/layouts/Default';
import { Alert } from 'components/templates/alert';

const AlertPage = () => {
  return (
    <Default pageName="Alert">
      <Alert />
    </Default>
  );
};

export default AlertPage;
